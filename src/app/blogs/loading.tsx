import Spinner from '@/components/ui/Spinner';
import React from 'react';

const LoadingPage = () => {
    return (
        <div>
            <Spinner></Spinner>
        </div>
    );
};

export default LoadingPage;